<?xml version="1.0" encoding="utf-8"?>
<!-- 찜한 장소 한 줄. 체크를 켜면 '찜한 곳으로 코스 만들기'의 대상이 된다. -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Ye.Card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/ye_space_xs"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="@dimen/ye_space_sm">

        <ImageView
            android:id="@+id/fav_check"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/ye_bg_circle_tint"
            android:backgroundTint="@color/ye_surface_variant"
            android:importantForAccessibility="no"
            android:padding="@dimen/ye_space_2xs"
            android:src="@drawable/ic_ye_check"
            app:tint="@color/ye_on_surface_faint" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/ye_space_sm"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/fav_title"
                style="@style/Ye.Text.TitleSmall"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                tools:text="서울숲" />

            <TextView
                android:id="@+id/fav_meta"
                style="@style/Ye.Text.Caption"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                tools:text="공원" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/fav_delete"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/my_fav_delete_cd"
            android:padding="@dimen/ye_space_xs"
            android:src="@drawable/ic_ye_heart_filled"
            app:tint="@color/ye_danger" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
